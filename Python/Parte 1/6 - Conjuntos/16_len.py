# # Define um conjunto de números com elementos duplicados
# numeros = {1, 2, 3, 1, 2, 4, 5, 5, 6, 7, 8, 9, 0}

# # Imprime o tamanho do conjunto (número de elementos únicos)
# print(len(numeros))  # Output: 10

# A função len() retorna o número de elementos em um objeto, que pode ser uma lista, tupla, conjunto, dicionário, etc. 
# No caso do conjunto numeros, ela retorna o número de elementos únicos, excluindo as duplicatas. 
# Portanto, neste exemplo, o tamanho do conjunto numeros é 10.
